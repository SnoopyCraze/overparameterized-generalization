# Implicit Regularization and Generalization in Overparameterized Neural Networks

Experiments on why overparameterized neural networks generalize despite having
far more parameters than training samples. PyTorch, MNIST and CIFAR-10.

Work in progress.
